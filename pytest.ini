[pytest]
markers =
    network: tests that require live network access (skipped by default;
        run with `pytest -m network`)

# Skip network-marked tests by default. Run them explicitly with
# `pytest -m network` when you have working internet and want to
# exercise live collector paths.
addopts = -m "not network"
